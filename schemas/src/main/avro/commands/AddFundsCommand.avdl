@namespace("parts.code.piggybox.schemas")
protocol PiggyBox {

  record AddFundsCommand {
    string id;
    timestamp_ms occurredOn;
    string customerId;
    decimal(4,2) amount;
    string currency;
  }
}
