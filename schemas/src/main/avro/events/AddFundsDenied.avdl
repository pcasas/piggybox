@namespace("parts.code.piggybox.schemas.events")
protocol PiggyBox {

  record AddFundsDenied {
    string id;
    timestamp_ms occurredOn;
    string customerId;
    decimal(4,2) amount;
    string currency;
  }
}
